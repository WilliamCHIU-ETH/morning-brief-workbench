node_modules/

# gate runner 的產出，每次執行都會重寫
gate-report.json

# 各階段在 project 目錄裡的產物（fixtures 裡的輸入檔要進版控，產物不要）
# fixtures 的 sidecar 要進版控（gate 會檢查它們存在）；只忽略非 fixtures 的
/*/**/*.provenance.json
!fixtures/**/*.provenance.json

# agent 的工作目錄
projects/

# ASR 模型 57MB，用 npm run setup:whisper 下載
.cache/
