# 複製成 .env（已 gitignore），把值填進去。
# 兩個 repo 刻意互不依賴，這份是 workbench 自己的，不要指向 marketing-video 的。

# HeyGen API key。只有主播生成（stages/heygen.mjs create/poll）需要。
# 取得：HeyGen 後台 → Settings → API
HEYGEN_API_KEY=

# 選填：覆寫預設的主播與聲音（見 contracts/avatar-generation.json 的 registry）
# MB_AVATAR_ID=
# MB_VOICE_ID=

# 選填：ASR 模型路徑。預設 .cache/whisper/ggml-base-q5_1.bin（npm run setup:whisper）
# MB_WHISPER_MODEL=
